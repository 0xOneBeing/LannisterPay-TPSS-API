module.export = function(app) {
    let id, amount, currency, customerEmail;
    app.post('./split-payments/compute', (res, req) => {
        // res.send(ID, Amount, Currency, CustomerEmail)
        function createSplit() {
            
        }
    })


}
